import os, sys

sys.path.append(os.path.dirname(".."))
from episode import Episode


class Analyser():
    def analyse(self, name, tokens) -> Episode:
        episode = Episode()
        return episode
